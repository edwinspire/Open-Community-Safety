//require("dotenv").config({ override: true });
const { OMS_USER, OMS_PASSWORD } = process.env;
module.exports = function Authentication() {
    return 'Basic ' + Buffer.from(OMS_USER + ":" + OMS_PASSWORD).toString('base64');
}